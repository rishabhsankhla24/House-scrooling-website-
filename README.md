# ALBA residence website

A responsive React + Vite showcase built around the supplied residence film.

## Run locally

```bash
pnpm install
pnpm dev
```

Create a production build with `pnpm build`.
